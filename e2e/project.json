{
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "name": "angular-electron-e2e",
  "projectType": "application",
  "targets": {
    "lint": {
      "executor": "@angular-eslint/builder:lint",
      "options": {
        "lintFilePatterns": [
          "e2e/**/*.ts"
        ]
      },
      "outputs": [
        "{options.outputFile}"
      ]
    }
  }
}
